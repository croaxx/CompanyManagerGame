<UserControl x:Class="Game.UI.View.ProjectsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Game.UI.View"
             xmlns:converter ="clr-namespace:Game.UI.Converter"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="300">

    <UserControl.Resources>
        <converter:PriceConverter x:Key="localPriceConverter"></converter:PriceConverter>
        <converter:ProgressForegroundConverter x:Key="ProgressForegroundConverter"></converter:ProgressForegroundConverter>
    </UserControl.Resources>

    <Grid>
        <ListView ItemsSource="{Binding Projects}" x:Name="ListView1">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Border BorderBrush="#CCCCCC" BorderThickness="2">
                        <StackPanel Orientation="Vertical">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="Project title: "></TextBlock>
                                <TextBlock Text="{Binding Title}"></TextBlock>
                            </StackPanel>
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="Started: "></TextBlock>
                                <TextBlock Text="{Binding StartTime, StringFormat='dd/MM/yyyy HH:mm::ss'}"></TextBlock>
                            </StackPanel>
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="Due: "></TextBlock>
                                <TextBlock Text="{Binding ExpiryTime, StringFormat='dd/MM/yyyy HH:mm::ss'}"></TextBlock>
                            </StackPanel>
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="Reward: "></TextBlock>
                                <TextBlock Text="{Binding Reward, Converter={StaticResource localPriceConverter}}"></TextBlock>
                            </StackPanel>
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="% time passed: "></TextBlock>
                                <ProgressBar Width ="100" Minimum="0" Maximum="100" Value="{Binding PercentTimePassed, Mode=OneWay}" Foreground="{Binding RelativeSource={RelativeSource Mode=Self}, Path=Value, Converter={StaticResource ProgressForegroundConverter}}" />
                            </StackPanel>
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="Work done: "></TextBlock>
                                <ProgressBar Width ="100" Minimum="0" Maximum="100" Value="70" />
                            </StackPanel>
                            <StackPanel>
                                <Button Command="{Binding ElementName=ListView1, Path=DataContext.RemoveProjectCommand}" CommandParameter="{Binding}" Width ="40" HorizontalAlignment="Center" Content="X"></Button>
                            </StackPanel>
                        </StackPanel>
                    </Border>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</UserControl>